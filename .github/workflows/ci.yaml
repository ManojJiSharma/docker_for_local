name: test

on: 
    push:
        branches:
        - master 

jobs: 
    matic-setup:
      runs-on: ubuntu-latest
      steps:
        - name: Checkout sources
          uses: actions/checkout@v4
        # - name: clone matic-cli
        #   run: git clone https://github.com/maticnetwork/matic-cli.git

        #   - name: Checkout sources
    #     uses: actions/checkout@v4
  
    #   - name: Install rust toolchain
    #     uses: actions-rs/toolchain@v1
    #     with:
    #       toolchain: stable
    #       components: rustfmt, clippy


    # test-client:
    #     runs-on: ubuntu-latest